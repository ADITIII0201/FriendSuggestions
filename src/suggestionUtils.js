// Extract all utility functions from your main component
export const validateUser = (user) => {
  // ... your validateUser implementation
};

export const calculateSuggestionScore = (user, currentUser, connections, weights) => {
  // ... your calculateSuggestionScore implementation
};

export const saveToStorage = (key, value) => {
  // ... your saveToStorage implementation
};

export const readFromStorage = (key, defaultValue) => {
  // ... your readFromStorage implementation
};

// Export all other utility functions...
